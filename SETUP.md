# 自動更新のセットアップ手順

このzipを展開すると、以下のファイルが入っています。

```
index.html                    ← 差し替え（自動更新エリアを追加済み）
auto.css                      ← 新規（自動更新エリアの見た目）
scripts/fetch.py              ← 新規（ニュース・順位表の取得）
scripts/build.py              ← 新規（HTMLの生成）
.github/workflows/update.yml  ← 新規（1日3回の自動実行）
```

`article-*.html` などの手書き記事には一切触れません。

---

## 1. ファイルをアップロードする

1. https://github.com/genjok0216kk/bleague-matome を開く
2. **Add file → Upload files** をクリック
3. 展開したフォルダの中身（`index.html` / `auto.css` / `scripts` フォルダ / `.github` フォルダ）を**まとめてドラッグ＆ドロップ**
4. 下の Commit changes を押す

> `.github` は隠しフォルダです。Finderで `Command + Shift + .` を押すと表示されます。

---

## 2. Actionsに書き込み権限を与える（重要）

これをやらないと、生成したHTMLをリポジトリに書き戻せません。

1. リポジトリの **Settings** タブ
2. 左メニュー **Actions → General**
3. 一番下の **Workflow permissions** で
   **「Read and write permissions」** を選択 → **Save**

---

## 3. 手動で1回動かして確認する

1. **Actions** タブ → 左メニューの **サイト自動更新**
2. 右側の **Run workflow** → 緑のボタンを押す
3. 1〜2分で完了。緑のチェックが付けばOK
4. https://genjok0216kk.github.io/bleague-matome/ を開いて
   「公式・メディアの新着」に記事が並んでいれば成功

うまくいかない場合は、Actionsの実行ログを開くと
`[fetch] B.LEAGUE公式: 20件` のような行が出ているはずです。
`0件` になっていたら取得元のHTML構造が変わったサインです。

---

## 4. 以降は自動

| 日本時間 | 内容 |
|---|---|
| 07:00 | 取得 → HTML生成 → 差分があれば自動コミット |
| 12:00 | 同上 |
| 21:00 | 同上 |

GitHub Pagesはコミットのたびに再デプロイされるので、
だいたい数分後にサイトへ反映されます。

---

## 何がどう更新されるのか

### 自動で入るもの

- **公式・メディアの新着**（トップページ・各カテゴリページ・`news.html`）
  B.LEAGUE公式サイトとバスケット・カウントの新着見出しを収集し、
  「移籍情報 / 試合結果 / 順位・日程 / 速報」に自動で振り分けます。
- **順位表**（サイドバー・`cat-data.html`）
  9月22日の開幕までは「開幕前」の案内が出ます。
  開幕後は勝敗・勝率・得失点差が自動で入ります。
- **ティッカー**（ページ上部の流れるやつ）
- **sitemap.xml**

### 自動で入らないもの

- 手書きの `article-*.html`
- ヒーロー記事、ピックアップ、人気記事ランキング、YouTube埋め込み

これらは今まで通り手で更新してください。

---

## 著作権まわりの方針

このしくみは **見出しとリンクだけ** を保存・掲載します。
記事本文の取得・保存・転載は一切行いません（`data/news.json` を見れば確認できます）。
リンクはすべて配信元へ飛ぶ外部リンク（`rel="noopener nofollow"`）です。

取得元の robots.txt は事前に確認済みで、いずれもクロールを禁止していません。
リクエスト間には2秒の待機を入れています。

---

## 取得元を増やしたいとき

`scripts/fetch.py` の以下の部分に追記してください。

```python
RSS_FEEDS = [
    ("バスケット・カウント", "https://basket-count.com/feed"),
    ("追加したいメディア名", "https://example.com/feed"),   # ← ここ
]
```

RSSがないサイトは個別に取得処理を書く必要があります。

---

## 更新頻度を変えたいとき

`.github/workflows/update.yml` の `cron` を書き換えます（**UTC表記**なので日本時間から9時間引く）。

```yaml
- cron: "0 22 * * *"   # JST 07:00
- cron: "0 3 * * *"    # JST 12:00
- cron: "0 12 * * *"   # JST 21:00
```

例：朝6時だけにしたいなら `- cron: "0 21 * * *"` の1行だけにします。
